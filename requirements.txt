# Dépendances principales
requests>=2.33.1,<3.0.0
beautifulsoup4>=4.14.3,<5.0.0
python-dotenv>=1.2.2,<2.0.0

# Utilitaires réseau
urllib3>=2.6.3,<3.0.0

# Enrichissement crédibilité sources (âge du domaine via WHOIS)
python-whois>=0.9.6,<1.0.0

# ── Dépendances optionnelles (optimisations techniques) ───────────────────────

# 2.1 — Couche analytique DuckDB (requêtes SQL sur JSON, 10-100× plus rapide)
# pip install duckdb>=1.5.2,<2.0.0
duckdb>=1.5.2,<2.0.0

# 2.4 — Parallélisme asyncio pour l'enrichissement (3-5× plus rapide)
# pip install aiohttp>=3.13.5,<4.0.0
aiohttp>=3.13.5,<4.0.0

# 2.8 — Batch API Anthropic Claude (NER + sentiment batch, ~50% d'économies)
# pip install anthropic>=0.96.0,<1.0.0
anthropic>=0.96.0,<1.0.0

# Tests
pytest>=9.0.3,<10.0.0
pytest-cov>=7.1.0,<8.0.0
